% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-write-file.R
\name{tool_write_file}
\alias{tool_write_file}
\title{Create a file writing tool}
\usage{
tool_write_file(
  allowed_dirs,
  max_file_size = "10MB",
  max_calls = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{allowed_dirs}{Character vector of directories the tool can write to.}

\item{max_file_size}{Max output file size. Default \code{"10MB"}.}

\item{max_calls}{Maximum invocations. \code{NULL} means unlimited.}

\item{overwrite}{Whether to allow overwriting existing files. Default \code{FALSE}.}
}
\value{
A \code{securer_tool} object.
}
\description{
Returns a \code{\link[securer:securer_tool]{securer::securer_tool()}} that writes data to files in
specified directories with size limits and overwrite protection.
}
\details{
The \code{content} argument type is declared as \code{"list"} in the tool schema
because the IPC serialization layer (JSON) converts most R objects to
lists. In practice, callers should pass:
\itemize{
\item A \code{data.frame} for CSV and JSON formats
\item A character vector for TXT format
\item Any R object for RDS format
}

Supported formats: csv, json, txt, rds. Format is auto-detected from
the file extension, or can be specified explicitly.

Security constraints:
\itemize{
\item \strong{Atomic writes}: Data is written to a temp file first,
validated for size, then moved to the target path.
\item \strong{Overwrite protection}: By default, existing files cannot
be overwritten (controlled by the \code{overwrite} parameter).
\item \strong{Symlink resolution}: Target paths are resolved via
\code{\link[base:normalizePath]{base::normalizePath()}} to prevent symlink-based directory escapes.
\item \strong{Size limits}: Written files exceeding \code{max_file_size}
are rejected before being moved to the target.
}
}
\examples{
\donttest{
tool <- tool_write_file(
  allowed_dirs = "/data/exports",
  max_file_size = "5MB",
  overwrite = FALSE
)
}
}
\seealso{
\code{\link[securer]{securer_tool}}, \code{\link{tool_read_file}}

Other tool factories: 
\code{\link{tool_calculator}()},
\code{\link{tool_data_profile}()},
\code{\link{tool_fetch_url}()},
\code{\link{tool_plot}()},
\code{\link{tool_query_sql}()},
\code{\link{tool_r_help}()},
\code{\link{tool_read_file}()}
}
\concept{tool factories}
